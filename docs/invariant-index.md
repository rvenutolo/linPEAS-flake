# Invariant index

Binding rules of the project. Each entry points to the tracked doc
that holds the canonical wording for that rule. Linted by
`scripts/check-orphan-invariants.sh` — every entry here must resolve
to an existing file under `docs/`, and every `docs/**/*.md` (minus
an explicit EXEMPT allowlist for overview pages) must appear here.

Behavior rules for the AI assistant and other non-binding guidance
live in `.claude/CLAUDE.md` (untracked).

## Security

- **Workflow SHA pinning** — every `uses:` SHA-pinned. → [security/repo-config.md](security/repo-config.md) <!-- enforcer: scripts/check-uses-sha-pinned.sh; ci: lint-workflow-security; hook: uses-sha-pinned -->
- **action pin comment** — every SHA-pinned `uses:` carries an exact patch-tag comment (`# vX.Y.Z`) — never absent, never a non-version comment, never a floating major (`# vX`) — with inline `# patch-tag-exception: <reason>` markers for refs without a patch tag. → [architecture/pin-convention.md](architecture/pin-convention.md) <!-- enforcer: scripts/check-patch-tag-pins.sh; ci: -; hook: patch-tag-pins -->
- **Pin digest provenance** — an action-pin SHA or container digest may not move under an unchanged version label (a repointed released tag); floating-major (`# vN`) digest moves must be reachable from the upstream default branch. → [security/repo-config.md](security/repo-config.md) <!-- enforcer: scripts/check-pin-digest-provenance.sh; ci: lint-doc-invariants; hook: - -->
- **Renovate invariants** — pinDigests, minimumReleaseAge, no top-level automerge. → [security/repo-config.md](security/repo-config.md) <!-- enforcer: scripts/check-renovate-invariants.sh, scripts/check-renovate-config-validator.sh; ci: renovate-invariants, lint-doc-invariants; hook: renovate-config-validator -->
- **No dead renovate markers** — every `# renovate:` marker's file is consumed by a live customManager (a filePattern scopes it and a matchString matches a line). → [security/repo-config.md](security/repo-config.md) <!-- enforcer: scripts/check-renovate-markers-matched.sh; ci: renovate-invariants; hook: - -->
- **Tag-protection ruleset** — `release-tag-protection`, drift-check lint. → [security/repo-config.md](security/repo-config.md) <!-- enforcer: scripts/check-tag-protection.sh; ci: tag-protection-drift-check; hook: - -->
- **Allowed-actions allowlist** — vendor list canonical; drift = incident. → [security/allowed-actions.md](security/allowed-actions.md) <!-- enforcer: scripts/check-allowed-actions-api.sh; ci: harness-group; hook: - -->
- **Bump-script integrity** — URL prefix, `.digest`, atomic write, API-version header. → [security/verification.md](security/verification.md) <!-- enforcer: scripts/check-gh-api-version-header.sh, scripts/check-bump-script-integrity.sh; ci: lint-script-hygiene; hook: gh-api-version-header, bump-script-integrity -->
- **verify-latest-release parity + attribution** — SRI drift = incident; per-reason notify bodies. → [security/verification.md](security/verification.md) <!-- enforcer: -; ci: -; hook: - -->
- **Failure-reason ladder coverage** — every `id:`-carrying step in the verify job is mapped to a reason token by the attribution ladder, in execution order, with every token documented; a step may opt out only with an inline `# reason-ladder-exempt: <reason>` marker. → [security/verification.md](security/verification.md) <!-- enforcer: scripts/check-verify-reason-ladder.sh; ci: lint-workflow-security; hook: verify-reason-ladder -->
- **Gitleaks / Dependency review / Trivy / Grype / SBOM** — scanner scope, thresholds, and notify-job split; Gitleaks and Dependency review are required checks, the CVE scans and SBOM attestation are advisory. → [security/verification.md](security/verification.md) <!-- enforcer: -; ci: -; hook: - -->
- **Cosign keyless signing + identity pinning** — per-arch + multi-arch index signed; verify must pin workflow ref + OIDC issuer. → [security/verification.md](security/verification.md) <!-- enforcer: scripts/check-cosign-identity-pinned.sh; ci: lint-workflow-security; hook: cosign-identity-pinned -->
- **gh attestation verify --repo pin** — every `gh attestation verify` invocation passes `--repo rvenutolo/linPEAS-flake`. → [security/verification.md](security/verification.md) <!-- enforcer: scripts/check-gh-attestation-repo.sh; ci: lint-workflow-security; hook: gh-attestation-repo -->
- **cosign verify identity + issuer pin** — every `cosign verify*` subcommand pins `--certificate-identity[-regexp]` AND `--certificate-oidc-issuer`. → [security/verification.md](security/verification.md) <!-- enforcer: scripts/check-cosign-identity-pinned.sh; ci: lint-workflow-security; hook: cosign-identity-pinned -->
- **Release-asset blob signatures** — every GitHub Release asset has a sibling `.sigstore` bundle signed via cosign keyless; verified at release time and weekly. → [security/verification.md](security/verification.md) <!-- enforcer: -; ci: -; hook: - -->
- **Release-asset provenance sidecar** — every GitHub Release asset has a sibling `.intoto.jsonl` SLSA build-provenance bundle (cosign keyless via `attest-build-provenance`); satisfies OSSF Scorecard `releasesHaveProvenance` probe. → [security/verification.md](security/verification.md) <!-- enforcer: -; ci: -; hook: - -->
- **Bump credentials blast-radius** — GitHub App, no PAT, no `git push`. → [security/trust-model.md](security/trust-model.md) <!-- enforcer: -; ci: -; hook: - -->
- **PR-triggered workflow secret allowlist** — only `secrets.GITHUB_TOKEN`. → [security/trust-model.md](security/trust-model.md) <!-- enforcer: scripts/check-pr-workflows-no-secrets.sh; ci: pr-workflows-no-secrets; hook: - -->
- **flake.lock input provenance** — bot flake.lock bumps may only move rev/narHash/lastModified; a top-level input add/remove/repoint or any node repointed at any depth fails the gate; the entry point is each lock's own top-level `.root` field (missing/non-string `.root` is an operational error, a base/head root-id mismatch fails closed) and top-level input refs resolve through `follows` paths (string = node id, array = path walked from that root, cycle-guarded) before comparison, an unresolvable ref fails closed, and a ref-shape change whose resolved source is unchanged passes by design. → [security/trust-model.md](security/trust-model.md) <!-- enforcer: scripts/check-flake-lock-provenance.sh; ci: lint-doc-invariants; hook: - -->
- **harden-runner** — first step in every job. → [security/trust-model.md](security/trust-model.md) <!-- enforcer: scripts/check-harden-runner-first.sh; ci: lint-workflow-security; hook: harden-runner-first -->
- **harden-runner block egress** — every harden-runner step uses `egress-policy: block` with a non-empty per-job `allowed-endpoints:`. → [security/trust-model.md](security/trust-model.md) <!-- enforcer: scripts/check-harden-runner-block.sh; ci: lint-workflow-security; hook: harden-runner-block -->
- **egress allowlist matches tool inventory** — every job's `allowed-endpoints` list carries the hosts its tool inventory reaches, a complete sigstore host set if it carries any sigstore host, the set declared beside the `notify-workflow-result` composite if the job runs it, and no host nothing in this repo reaches. → [security/trust-model.md](security/trust-model.md) <!-- enforcer: scripts/check-egress-allowlist.sh; ci: lint-workflow-security; hook: egress-allowlist -->
- **Per-job timeout-minutes** — every job declares explicit `timeout-minutes`. → [security/workflow-hardening.md](security/workflow-hardening.md) <!-- enforcer: scripts/check-job-timeout-minutes.sh; ci: lint-workflow-security; hook: job-timeout-minutes -->
- **Workflow concurrency group** — every workflow declares top-level `concurrency.group`. → [security/workflow-hardening.md](security/workflow-hardening.md) <!-- enforcer: scripts/check-workflow-concurrency.sh; ci: lint-workflow-security; hook: workflow-concurrency -->
- **Checkout persist-credentials false** — every `actions/checkout` step sets `persist-credentials: false`. → [security/workflow-hardening.md](security/workflow-hardening.md) <!-- enforcer: scripts/check-checkout-persist-credentials.sh; ci: lint-workflow-security; hook: checkout-persist-credentials -->
- **upload-artifact strict** — every `actions/upload-artifact` step sets `if-no-files-found: error`. → [security/workflow-hardening.md](security/workflow-hardening.md) <!-- enforcer: scripts/check-upload-artifact-strict.sh; ci: lint-workflow-security; hook: upload-artifact-strict -->
- **Workflow on.branches main-only** — every `pull_request:`/`push:` trigger declares `branches: [main]` exactly. → [security/workflow-hardening.md](security/workflow-hardening.md) <!-- enforcer: scripts/check-workflow-on-branches.sh; ci: lint-workflow-security; hook: workflow-on-branches -->
- **pull_request_target forbidden** — no workflow uses the `pull_request_target` trigger. → [security/workflow-hardening.md](security/workflow-hardening.md) <!-- enforcer: scripts/check-pull-request-target-absent.sh; ci: lint-workflow-security; hook: pull-request-target-absent -->
- **Auto-merge decline gate** — every workflow run-block calling `gh pr merge --auto` inspects PR state and aborts on a `CLOSED`/`MERGED` PR so a declined or merged PR is never resurrected. → [security/workflow-hardening.md](security/workflow-hardening.md) <!-- enforcer: scripts/check-auto-merge-decline-gate.sh; ci: lint-workflow-security; hook: auto-merge-decline-gate -->
- **scripts/\*.sh shebang + pipefail** — every executable under `scripts/` starts with `#!/usr/bin/env bash` and uses `set -Eeuo pipefail`, while every sourced library under `scripts/lib/` satisfies the inverse: a `shellcheck shell=` directive, no shebang, no shell options of its own. → [security/workflow-hardening.md](security/workflow-hardening.md) <!-- enforcer: scripts/check-script-shebang-pipefail.sh; ci: lint-script-hygiene; hook: script-shebang-pipefail -->
- **No opaque process-substitution loops in scripts** — no script anywhere under `scripts/` — including the sourced libraries in `scripts/lib/` — feeds a redirection from a process substitution, whatever the producer, because the producer's exit status stays in its own subshell; `diff <(...) <(...)` stays legal. → [security/workflow-hardening.md](security/workflow-hardening.md) <!-- enforcer: scripts/check-no-opaque-procsub.sh; ci: lint-script-hygiene; hook: no-opaque-procsub -->
- **Library sourcing is tool-free** — a `source`/`.` library path never resolves through a command substitution of any kind, and `BASH_SOURCE` never appears inside a command substitution anywhere else under `scripts/` either, so a script's own directory resolves by parameter expansion (`${BASH_SOURCE[0]%/*}`), never through something that needs `readlink`, `dirname`, or any other tool on PATH. → [security/workflow-hardening.md](security/workflow-hardening.md) <!-- enforcer: scripts/check-lib-source-tool-free.sh; ci: lint-script-hygiene; hook: - -->
- **Could-not-run guards exit 2** — no script anywhere under `scripts/`, libraries included, exits 1 out of a guard whose test is purely an availability check (`! command -v X`, `! require_tool X`, `[[ ! -f|-r|-e|-d|-s PATH ]]`, `[[ -f PATH ]] || { … }`); a guard whose missing input is itself the finding opts out with an inline `# exit-code-exempt: <rationale>` marker. → [security/workflow-hardening.md](security/workflow-hardening.md) <!-- enforcer: scripts/check-guard-exit-code.sh; ci: lint-script-hygiene; hook: - -->
- **Temp files created through the guarded helper** — no script anywhere under `scripts/`, libraries included, creates a temp file with a bare `mktemp`; every creation runs through `make_temp` (`scripts/lib/temp.sh`), which reports an unwritable `TMPDIR` as exit 2 instead of letting `mktemp`'s own exit 1 read as a content violation, and a call whose failure is itself the finding opts out with an inline `# exit-code-exempt: <rationale>` marker. → [security/workflow-hardening.md](security/workflow-hardening.md) <!-- enforcer: scripts/check-guard-exit-code.sh; ci: lint-script-hygiene; hook: - -->
- **No control characters in tracked paths** — no path git reports for the tree (tracked, or untracked-and-unignored) carries a byte in the 0x01-0x1F control range or DEL (0x7F). → [security/workflow-hardening.md](security/workflow-hardening.md) <!-- enforcer: scripts/check-path-hygiene.sh; ci: lint-script-hygiene; hook: - -->
- **awk file operands spelled explicit** — every `awk` invocation in `scripts/*.sh` carrying a file operand spells it `"$(awk_path "${var}")"`, since `awk` reads a bare operand shaped `name=value` as a variable assignment rather than a filename and silently scans nothing. → [security/workflow-hardening.md](security/workflow-hardening.md) <!-- enforcer: scripts/check-awk-operand-explicit.sh; ci: lint-script-hygiene; hook: - -->
- **Filesystem enumerations run through the helper** — every `find`, `git ls-files` and `git ls-tree` in a repo script is an argument to `enumerate_into`, or runs inside a function that helper is handed by name, or carries an inline `# enumerate-exempt: <rationale>` marker; the helper asserts scan breadth, which a producer's exit status cannot, since one that enumerates nothing still exits 0. → [security/workflow-hardening.md](security/workflow-hardening.md) <!-- enforcer: scripts/check-enumerate-helper-required.sh; ci: lint-script-hygiene; hook: - -->
- **Glob-driven scans run through the helper** — no `for` loop under `scripts/` expands a glob at its own loop head; every glob-driven scan fills its array through `glob_into` (`scripts/lib/enumerate.sh`), or carries an inline `# glob-exempt: <rationale>` marker naming an empty match set as that loop's normal state, since a pattern matching nothing expands to nothing and a body that never runs finds no violation to report. → [security/workflow-hardening.md](security/workflow-hardening.md) <!-- enforcer: scripts/check-enumerate-helper-required.sh; ci: lint-script-hygiene; hook: - -->
- **Payload shape-gate scenario coverage** — every script matching the four-arm external-payload predicate (`gh api`, a `*_JSON_OVERRIDE` variable, a bare stdin slurp, or a scoped `flake.lock` read) carries a harness scenario asserting exit 2 against a malformed payload — anchored to the scenario call's own bare positional exit-code argument, never a textual `2` substring — or a `# payload-subject-exempt: <rationale>` marker; a marker on a script the predicate does not match is reported as drift. → [security/workflow-hardening.md](security/workflow-hardening.md) <!-- enforcer: scripts/check-payload-shape-scenario.sh; ci: lint-script-hygiene; hook: - -->
- **check-script ↔ test pairing** — every `scripts/check-*.sh` has a matching `tests/check-*.test.sh` and vice versa. → [security/workflow-hardening.md](security/workflow-hardening.md) <!-- enforcer: scripts/check-script-has-test.sh; ci: lint-script-hygiene; hook: script-has-test -->
- **test-runner reachability** — every `tests/*.test.sh` harness is executed by some runner (the `run-harness-group` HARNESSES array, the `refresh-*.test.sh` doc-freshness glob, a `lint-groups.yml` member, or a direct workflow invocation). → [security/workflow-hardening.md](security/workflow-hardening.md) <!-- enforcer: scripts/check-test-reachable.sh; ci: lint-script-hygiene; hook: - -->
- **harness assertion discrimination** — a harness scenario's expected substring must not appear in any sibling scenario's output, and every output-asserting harness is wired to the gate. → [security/workflow-hardening.md](security/workflow-hardening.md) <!-- enforcer: -; ci: harness-group; hook: - -->
- **harness exemption ratchet** — no harness registers a `harness_assert_exempt` discrimination exemption, and a harness counts as wired only when it calls the verify function rather than naming it. → [security/workflow-hardening.md](security/workflow-hardening.md) <!-- enforcer: -; ci: harness-group; hook: - -->
- **harness census parity** — no two scenarios in a harness share one whole observable outcome (exit code, stdout and stderr together) unless a rationale-bearing `harness_assert_parity_exempt` covers the pair, and only an allowlisted harness may register one. → [security/workflow-hardening.md](security/workflow-hardening.md) <!-- enforcer: -; ci: harness-group; hook: - -->
- **manifest-reaching hook watches nix/hooks** — every pre-commit hook that reaches the Nix hook manifest (`preCommitHooks`), whether through the script its entry runs or through a flake attribute its entry evaluates, includes `nix/hooks` in its `files` filter, so a hook-definition edit re-triggers the freshness check on the per-changed-file commit path. → [security/workflow-hardening.md](security/workflow-hardening.md) <!-- enforcer: scripts/check-manifest-hook-watches-nix.sh; ci: lint-script-hygiene; hook: manifest-hook-watches-nix -->
- **freshness hook watches evaluated sources** — every pre-commit hook whose entry evaluates a flake attribute names, in its `files` filter, every source that evaluation reads: for a generator evaluating `devTooling.<system>.<attr>`, the nix modules the attribute is defined or transposed by; for an entry evaluating an attribute directly, `flake.nix`, `flake.lock`, the modules assigning it, and the paths those modules reference. A source edit then re-triggers the freshness check on the per-changed-file commit path. → [security/workflow-hardening.md](security/workflow-hardening.md) <!-- enforcer: scripts/check-freshness-hook-watches-modules.sh; ci: lint-script-hygiene; hook: freshness-hook-watches-modules -->
- **lean lint-shell routing** — light lint groups (`lint-workflow-security`, `lint-script-hygiene`) run in `devShells.lint`; `lint-doc-invariants` stays on `devShells.default` because `renovate-config-validator` needs the renovate closure. → [security/workflow-hardening.md](security/workflow-hardening.md) <!-- enforcer: -; ci: -; hook: - -->
- **lean lint-shell tool guard** — every tool the `.#lint`-hosted lint groups need is present on `PATH`; keeps `devShells.lint` buildInputs from silently dropping a tool. → [security/workflow-hardening.md](security/workflow-hardening.md) <!-- enforcer: scripts/check-lint-shell-tools.sh; ci: lint-script-hygiene; hook: lint-shell-tools -->
- **ci.yml job ↔ summary category** — every required ci.yml job appears in `docs/_data/ci-check-categories.yml`; every category entry points at a real job. → [security/workflow-hardening.md](security/workflow-hardening.md) <!-- enforcer: scripts/check-ci-job-in-summary.sh; ci: lint-doc-invariants; hook: ci-job-in-summary -->
- **Multi-line run: strict-mode prelude** — every block-scalar or newline-carrying `run:` block, in a workflow job or a `.github/actions/**/action.ya?ml` composite, starts with `set -Eeuo pipefail`. → [security/workflow-hardening.md](security/workflow-hardening.md) <!-- enforcer: scripts/check-run-block-strict.sh; ci: lint-workflow-security; hook: run-block-strict -->
- **Fork-guard on guard-required jobs** — every job holding `contents`/`packages`/`id-token`/`attestations`/`actions` at `write`, or minting a GitHub App installation token, includes `github.repository == 'rvenutolo/linPEAS-flake'` in its `if:`. → [security/workflow-hardening.md](security/workflow-hardening.md) <!-- enforcer: scripts/check-fork-guard-release.sh; ci: lint-workflow-security; hook: fork-guard-release -->
- **No unpinned `nix run nixpkgs#`** — no `nix` subcommand may take a bare `nixpkgs#<pkg>` ref; must use `nix shell .#<pkg>`, `nix run .#<pkg>`, or pin a revision. → [security/workflow-hardening.md](security/workflow-hardening.md) <!-- enforcer: scripts/check-nix-run-pinned.sh; ci: lint-workflow-security; hook: nix-run-pinned -->
- **setup-nix composite required** — workflows install Nix only via .github/actions/setup-nix with github-token. → [security/workflow-hardening.md](security/workflow-hardening.md) <!-- enforcer: scripts/check-setup-nix-required.sh; ci: setup-nix-required; hook: - -->
- **GITHUB_TOKEN min-permissions** — top-level `permissions: {}`; every job declares own scopes. → [security/min-permissions.md](security/min-permissions.md) <!-- enforcer: scripts/check-min-permissions.sh; ci: lint-workflow-security; hook: min-permissions -->
- **GITHUB_TOKEN write-scope allowlist** — every job's `write` scopes are pinned per job in `.github/permission-scopes.yml`. → [security/min-permissions.md](security/min-permissions.md) <!-- enforcer: scripts/check-permission-scopes.sh; ci: lint-workflow-security; hook: permission-scopes -->
- **protect-main ruleset** — in-tree mirror + drift-check; mirror also parity-checked against the required-checks doc table. → [security/required-checks.md](security/required-checks.md) <!-- enforcer: scripts/check-protect-main.sh; ci: protect-main-drift-check; hook: - -->
- **Repo-settings posture** — probe-verifiable security_and_analysis, Actions-permissions, and workflow-token flags. → [security/settings-posture.md](security/settings-posture.md) <!-- enforcer: scripts/check-settings-posture.sh; ci: harness-group; hook: - -->
- **Workflow-scanner division of labor** — layered DiD model (pre-commit → PR/push → weekly sweep → watchdog) across zizmor/codeql/octoscan/scorecard plus in-tree lints; records why the overlap is budgeted. → [security/workflow-scanners.md](security/workflow-scanners.md) <!-- enforcer: -; ci: -; hook: - -->

## Architecture / CI

- **Pin-diff isolation** — only `bump-linpeas.sh` mutates `linpeas-pin.json`. → [architecture/auto-update.md](architecture/auto-update.md) <!-- enforcer: scripts/check-pin-diff-isolated.sh; ci: lint-doc-invariants; hook: pin-diff-isolated -->
- **flake.nix pin invariants** — `pin.version` regex, `pin.url` prefix. → [architecture/auto-update.md](architecture/auto-update.md) <!-- enforcer: -; ci: -; hook: - -->
- **Release VERSION shape** — `^[0-9]{8}-[0-9a-f]{7,40}$`. → [architecture/auto-update.md](architecture/auto-update.md) <!-- enforcer: -; ci: -; hook: - -->
- **Linpeas-pin release-trigger** — pin change must cut release. → [architecture/auto-update.md](architecture/auto-update.md) <!-- enforcer: -; ci: -; hook: - -->
- **Stale-pin failure attribution** — `stale-pin-check.yml` notify body distinguishes `reason=upstream-api-failure` from `reason=stall-detected`; never collapse into a single failure class. → [architecture/ci.md](architecture/ci.md) <!-- enforcer: -; ci: -; hook: - -->
- **Cron-notify root-cause comments** — when a `notify-workflow-result` issue auto-closes after a transient failure recovers, a one-line root-cause comment goes on the closed issue; maintainer discipline, no lint. → [architecture/ci.md](architecture/ci.md) <!-- enforcer: -; ci: -; hook: - -->
- **dockerhub-sync trigger** — triggers are `workflow_run` of `release-on-bump` completed-successfully plus manual `workflow_dispatch`; no `push:` trigger; the `if:` gate stays on the `sync` job. → [architecture/ci.md](architecture/ci.md) <!-- enforcer: -; ci: -; hook: - -->
- **Pages site invariants** — `docs/_data/dashboard.yml` is generated and gitignored; `gen-dashboard-data.sh` hard-fails on bad pin shape or missing JSON fields, and shape-checks every soft this-repo lookup so an API error body degrades to the documented fallback instead of publishing a literal `"null"`. → [architecture/ci.md](architecture/ci.md) <!-- enforcer: scripts/gen-dashboard-data.sh; ci: dashboard-data-tests; hook: - -->
- **Cron schedule** — cron schedule table in docs/architecture/ci.md must match workflow cron triggers. → [architecture/ci.md](architecture/ci.md) <!-- enforcer: scripts/check-cron-table.sh; ci: -; hook: check-cron-table -->
- **Single-source cron schedules** — workflow cron times live only in the ci.md schedule table; other docs link it, never restate a literal HH:MM. → [architecture/ci.md](architecture/ci.md) <!-- enforcer: scripts/check-doc-cron-restatement.sh; ci: lint-doc-invariants; hook: check-doc-cron-restatement -->
- **update-flake-lock credential split / renovate-flake-lock-refresh** → [architecture/flake-input-bumps.md](architecture/flake-input-bumps.md) <!-- enforcer: -; ci: -; hook: - -->
- **Lock-derived doc regeneration** — every workflow that writes a `flake.lock` runs a generator for each freshness hook that declares `flake.lock` a trigger, and its committable-path list carries `flake.lock`. → [architecture/flake-input-bumps.md](architecture/flake-input-bumps.md) <!-- enforcer: scripts/check-lock-derived-docs.sh; ci: lint-workflow-security; hook: lock-derived-docs -->

## Install / Runbooks

- **Declared systems evaluate** — every system in `flake.lib.systems` force-evaluates in CI down to each package's derivation, failing with the system named. → [install/nix.md](install/nix.md) <!-- enforcer: scripts/check-flake-systems-eval.sh; ci: flake-check; hook: - -->
- **OCI image** — `Entrypoint` not `Cmd`; bash+coreutils set. → [install/docker.md](install/docker.md) <!-- enforcer: -; ci: -; hook: - -->
- **Manifest digest-pinning** — every multi-arch manifest-creating docker command takes its source refs as `@sha256:` digests, never mutable tags. → [install/docker.md](install/docker.md) <!-- enforcer: scripts/check-manifest-digest-pinned.sh; ci: lint-workflow-security; hook: manifest-digest-pinned -->
- **DOCKERHUB_TOKEN split** — `_RW` vs `_DELETE`, never unsuffixed. → [runbooks/dockerhub-recovery.md](runbooks/dockerhub-recovery.md) <!-- enforcer: scripts/check-dockerhub-token-scope-split.sh; ci: lint-doc-invariants; hook: - -->
- **Docker Hub notify-body parity** — issue body mirrors runbook. → [runbooks/dockerhub-recovery.md](runbooks/dockerhub-recovery.md) <!-- enforcer: -; ci: -; hook: - -->
- **settings-drift-checker App scope** — dedicated read-only App for admin-scoped settings probes; isolates blast radius from GITHUB_TOKEN and the bump App. → [runbooks/settings-drift-app.md](runbooks/settings-drift-app.md) <!-- enforcer: -; ci: -; hook: - -->
- **Ratchet pin audit** — daily detection of tag-vs-SHA drift on pinned action refs; deduped `ratchet-drift` umbrella issue auto-opens/closes. → [runbooks/ratchet-pin-audit.md](runbooks/ratchet-pin-audit.md) <!-- enforcer: scripts/check-ratchet-pin-audit.sh; ci: harness-group; hook: - -->
- **Reproducibility check** — weekly dual-build comparison; diffoscope diff + auto-issue on mismatch; burn-in → required promotion criteria. → [runbooks/reproducibility-check.md](runbooks/reproducibility-check.md) <!-- enforcer: -; ci: -; hook: - -->
- **Scorecard drift check** — weekly OSSF Scorecard run across a curated 11-check allowlist; any check scoring \<10 fails the workflow and opens a deduped `scorecard-drift` tracking issue. Uses a fine-grained `SCORECARD_PAT` (scoped to linPEAS-flake only, Read-only on Contents/Metadata/Webhooks) because the Webhooks check exceeds GITHUB_TOKEN scope; rotated annually. → [runbooks/scorecard-pat-rotation.md](runbooks/scorecard-pat-rotation.md) <!-- enforcer: scripts/check-scorecard-threshold.sh; ci: -; hook: - -->
- **Scorecard Signed-Releases backfill** — recovery procedure when historic releases lack `.sigstore` / `.intoto.jsonl` sidecars; runbook drives the `backfill-tag` workflow_dispatch input. → [runbooks/scorecard-signed-releases-backfill.md](runbooks/scorecard-signed-releases-backfill.md) <!-- enforcer: -; ci: -; hook: - -->
- **CI retry watchdog** — re-runs failed jobs on open bot-authored auto-merge PRs, bounded to 3 attempts per run via `run_attempt` (no stored state); escalates to a deduped-by-title issue on exhausting the bound; a per-PR error is confined to that PR and fails the job at the end rather than aborting the sweep. → [runbooks/ci-watchdog.md](runbooks/ci-watchdog.md) <!-- enforcer: -; ci: -; hook: - -->
- **actionlint embedded-shellcheck pin** — `nix/wrappers.nix` wraps the actionlint binary with an explicit `-shellcheck=...` flag, and the hook in `nix/hooks/linters.nix` consumes that wrapper, so embedded coverage does not silently degrade outside the devShell; canary smoke-tests the wiring; python `run:` blocks are guarded out until pyflakes is wired the same way. → [actionlint-embedded-linters.md](actionlint-embedded-linters.md) <!-- enforcer: scripts/check-actionlint-shellcheck-active.sh, scripts/check-run-block-pyflakes-required.sh; ci: -; hook: actionlint-shellcheck-active, check-run-block-pyflakes-required -->
- **octoscan workflow scanning** — synacktiv/octoscan scans `.github/workflows/` via a pinned ghcr.io container image; digest + version label pinned in `scripts/octoscan-scan.sh` (Renovate-managed). → [development/linting.md](development/linting.md) <!-- enforcer: scripts/octoscan-scan.sh; ci: -; hook: octoscan -->

## Development

- **ci-summary-category-map** — every required status check is categorized for the README CI summary. → [security/required-checks.md](security/required-checks.md). <!-- enforcer: scripts/check-ci-job-in-summary.sh, scripts/refresh-ci-summary.sh; ci: lint-doc-invariants, doc-freshness; hook: ci-job-in-summary, ci-summary-fresh -->
- **Merging PRs** — merge-commit only, signed, PR title = subject. → [development/git.md](development/git.md) <!-- enforcer: -; ci: -; hook: commitlint -->
- **Commitlint config is explicit** — every commitlint action step names an existing `configFile`, and the merge ruleset relaxes only the two line-length rules. → [development/git.md](development/git.md) <!-- enforcer: scripts/check-commitlint-config-explicit.sh; ci: lint-workflow-security; hook: commitlint-config-explicit -->
- **Ephemeral-reference ban** — every Markdown, shell, Nix and YAML source in the repo outside the skip allowlist carries no PR/issue refs, prose dates, planning/review labels, or literal `.claude/` paths. → [development/linting.md](development/linting.md) <!-- enforcer: scripts/check-ephemeral-refs.sh; ci: lint-doc-invariants; hook: check-ephemeral-refs -->
- **Treefmt YAML quote gotcha / flake-show auto-block** → [development/linting.md](development/linting.md) <!-- enforcer: scripts/refresh-flake-show.sh; ci: -; hook: treefmt, flake-show-fresh -->
- **Flake outputs reference** — auto-regenerated `nix flake show --all-systems` tree; do not hand-edit. → [reference/flake-outputs.md](reference/flake-outputs.md) <!-- enforcer: scripts/refresh-flake-show.sh; ci: doc-freshness; hook: flake-show-fresh -->
- **PR auto-labeling** → [development/labeling.md](development/labeling.md) <!-- enforcer: -; ci: -; hook: - -->
- **Changelog tag-pattern parity** — `cliff.toml` `tag_pattern` matches the canonical pin-shape regex enforced across the bump/release/dashboard scripts. → [development/changelog.md](development/changelog.md) <!-- enforcer: scripts/check-cliff-tag-pattern.sh; ci: cliff-tag-pattern; hook: - -->
- **Changelog link-duplication guard** — the regenerated changelog contains zero duplicate identical PR links and retains the scorecard-count preprocessor. → [development/changelog.md](development/changelog.md) <!-- enforcer: scripts/check-changelog-links.sh; ci: changelog-links; hook: - -->
- **Changelog released-section freshness** — `CHANGELOG.md`'s released sections match a fresh git-cliff regeneration for every release tag predating the last changelog commit, so a release that ships without the changelog job landing (or a manual edit to a released section) is caught rather than accruing silently. → [development/changelog.md](development/changelog.md) <!-- enforcer: scripts/check-changelog-fresh.sh; ci: changelog-links; hook: - -->
- **Changelog generator-owned** — `CHANGELOG.md` is regenerated by `release-on-bump.yml changelog` job; manual edits are a review-blocker. → [development/changelog.md](development/changelog.md) <!-- enforcer: -; ci: -; hook: - -->
- **Changelog App identity** — changelog commit-back uses the same GitHub App identity as `update-linpeas.yml push-and-merge`; no new secret surface. → [development/changelog.md](development/changelog.md) <!-- enforcer: -; ci: -; hook: - -->
- **Changelog self-reference skip** — `cliff.toml` MUST retain the `^docs: update changelog` commit-parser skip rule. → [development/changelog.md](development/changelog.md) <!-- enforcer: -; ci: -; hook: - -->
- **Size-label ignore-list parity** — the size-label ignore list holds exactly the paths generators declare with `@generates`, never a `@generates-block` path, and nothing else but the lint's named exemptions. → [development/labeling.md](development/labeling.md) <!-- enforcer: scripts/check-size-label-ignores.sh; ci: lint-doc-invariants; hook: size-label-ignores -->
- **Cache-prune permission scope** — `actions-cache-prune.yml` jobs declare only `actions: write` + `contents: read`. → [development/cache-cleanup.md](development/cache-cleanup.md) <!-- enforcer: scripts/check-permission-scopes.sh; ci: lint-workflow-security; hook: permission-scopes -->
- **Cache-prune fork-guard** — every job in `actions-cache-prune.yml` includes `github.repository ==` fork-guard. → [development/cache-cleanup.md](development/cache-cleanup.md) <!-- enforcer: scripts/check-fork-guard-release.sh; ci: lint-workflow-security; hook: fork-guard-release -->

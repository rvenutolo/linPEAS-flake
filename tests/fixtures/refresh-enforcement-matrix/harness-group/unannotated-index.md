# Invariant index (fixture)

## Security

- **Enforced elsewhere** — names the same enforcer but attributes it to a different job, so a roster that runs it in the harness group is unannotated. → [security/repo-config.md](security/repo-config.md) <!-- enforcer: scripts/check-thing.sh; ci: lint-script-hygiene; hook: - -->

# Invariant index (fixture)

## Security

- **Enforced by the harness group** — claims the `harness-group` job enforces it. Whether that is true depends on the roster the run supplies, not on the job existing. → [security/repo-config.md](security/repo-config.md) <!-- enforcer: scripts/check-thing.sh; ci: harness-group; hook: - -->

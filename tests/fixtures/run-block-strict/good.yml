name: good
on: push
permissions: {}
concurrency:
  group: good-${{ github.ref }}
  cancel-in-progress: true
jobs:
  a:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
    steps:
      - name: multi-line good
        run: |
          set -Eeuo pipefail
          echo one
          echo two
      - name: single line ok
        run: echo single

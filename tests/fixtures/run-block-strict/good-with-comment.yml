name: good-with-comment
on: push
permissions: {}
concurrency:
  group: good-with-comment-${{ github.ref }}
  cancel-in-progress: true
jobs:
  a:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
    steps:
      - name: comment first then set
        run: |
          # explain why
          set -Eeuo pipefail
          echo ok

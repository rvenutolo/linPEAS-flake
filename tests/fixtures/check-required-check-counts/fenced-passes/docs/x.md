# Doc

Write the marker as `3 <!-- count: required-contexts -->` or `<!-- count: bogus -->`, never as
`3 required checks` in running text.

```text
Every PR must pass 3 required checks.
Every PR must pass 4 <!-- count: required-contexts --> required checks.
```

> ```text
> 5 required contexts
> ```

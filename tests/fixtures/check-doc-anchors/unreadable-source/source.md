# Source

The harness strips this file's read permission before the run, so grep exits
2 on it. The link below would otherwise resolve cleanly, which is what makes
the unread run distinguishable from a run that read it.

[link](docs/target.md#real-section)

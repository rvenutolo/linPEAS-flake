Real prose naming the `flake-check` job resolves and is scanned.

```yaml
# The `ghost-in-a-fence` job below runs nowhere, and quoting it here is a
# showing rather than a claim, so the fence keeps it out of the scan.
jobs:
  ghost-in-a-fence:
```

name: bad-empty-group
on: push
permissions: {}
concurrency:
  group: ""
  cancel-in-progress: true
jobs:
  a:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
    steps:
      - run: echo a

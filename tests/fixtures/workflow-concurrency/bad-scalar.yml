name: bad-scalar
on: push
permissions: {}
concurrency: just-a-string
jobs:
  a:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
    steps:
      - run: echo a

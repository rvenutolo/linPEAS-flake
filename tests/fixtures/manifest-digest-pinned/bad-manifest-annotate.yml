name: bad-manifest-annotate
jobs:
  manifest:
    steps:
      - name: annotate list
        run: |
          docker manifest annotate "ghcr.io/rvenutolo/linpeas:${VERSION}" \
            "ghcr.io/rvenutolo/linpeas:${VERSION}-arm64" \
            --arch arm64 --os linux

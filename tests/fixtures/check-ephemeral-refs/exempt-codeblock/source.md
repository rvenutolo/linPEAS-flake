# Exempt code blocks

A fenced block holding banned shapes must not trip the lint:

```
fixed in #123 on 2026-06-18 during Phase 2 review (D3)
path .claude/foo.md
```

An inline span like `#123` or `2026-06-18` is also exempt.

# Encoding shapes are not planning labels

Text encoded as UTF-8 and a PDF-1.7 file with EXIF-2 metadata.

Identifiers like ID5: and 10D5: are not review-pass labels.

A quoted `#123` keeps the file in the candidate set, so the shapes
above are scanned rather than set aside.

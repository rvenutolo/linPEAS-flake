# Advisory causal phrase

This check was previously enforced by human review only.

A quoted `#123` keeps the file in the candidate set, so the blocking
pass reads this prose instead of setting it aside unscanned.

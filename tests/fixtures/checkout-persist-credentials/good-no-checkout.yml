name: good-no-checkout
on: push
permissions: {}
concurrency:
  group: good-no-checkout-${{ github.ref }}
  cancel-in-progress: true
jobs:
  a:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
    steps:
      - run: echo hello

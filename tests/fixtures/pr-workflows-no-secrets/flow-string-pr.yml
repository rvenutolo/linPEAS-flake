# tests/fixtures/pr-workflows-no-secrets/flow-string-pr.yml
name: flow-string-pr
on: pull_request
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: noop
        env:
          DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}
        run: "true"

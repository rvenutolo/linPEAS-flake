# tests/fixtures/pr-workflows-no-secrets/secrets-leak-pr-workflow.yml
name: secrets-leak-pr
on:
  pull_request:
    branches: [main]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: noop
        env:
          DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}
        run: 'true'

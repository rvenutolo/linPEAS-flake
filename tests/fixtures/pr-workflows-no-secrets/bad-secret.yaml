# tests/fixtures/pr-workflows-no-secrets/bad-secret.yaml
name: bad-secret
on:
  pull_request:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: noop
        env:
          TOKEN: ${{ secrets.SUPER_SECRET }}
        run: "true"

# tests/fixtures/pr-workflows-no-secrets/clean-pr-workflow.yml
name: clean-pr
on:
  pull_request:
    branches: [main]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: noop
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: 'true'

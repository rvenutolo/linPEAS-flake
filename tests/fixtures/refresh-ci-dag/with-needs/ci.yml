name: ci
on: push
jobs:
  job-a:
    runs-on: ubuntu-latest
    steps:
      - run: "true"
  job-b:
    runs-on: ubuntu-latest
    needs: job-a
    steps:
      - run: "true"
  job-c:
    runs-on: ubuntu-latest
    needs: [job-a, job-b]
    steps:
      - run: "true"

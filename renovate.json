{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:recommended", "helpers:pinGitHubActionDigests"],
  "schedule": ["before 06:00 on friday"],
  "timezone": "UTC",
  "platformAutomerge": true,
  "minimumReleaseAge": "7 days",
  "prConcurrentLimit": 3,
  "dependencyDashboard": false,
  "labels": ["dependencies"],
  "packageRules": [
    {
      "description": "github-actions: pin SHAs and auto-merge after the 7-day quarantine. Per-manager automerge scope (P2.1, GAP-21) ensures a future manager activation (e.g. npm via a new package.json) does NOT inherit automerge.",
      "matchManagers": ["github-actions"],
      "groupName": "github-actions",
      "pinDigests": true,
      "automerge": true
    },
    {
      "description": "Self-reference to local composite action pinned by SHA (CIW-1). Renovate's github-actions manager treats `owner/repo/path@sha` uniformly, so pinDigests above covers the bump path. This rule exists to document intent and to ensure the self-reference is not accidentally excluded by a future matcher.",
      "matchManagers": ["github-actions"],
      "matchPackageNames": ["rvenutolo/linPEAS-flake"],
      "pinDigests": true,
      "automerge": true
    },
    {
      "description": "Nix install URL: scoped automerge for the same reason as github-actions. The custom manager below extracts the version from the install_url string. matchDepNames is narrowed to 'nix' so the cachix/git-hooks.nix custom-regex manager (GAP-20) does NOT inherit automerge — that pin must be merged manually while the nixpkgs 25.05→25.11 bump is in flight.",
      "matchManagers": ["custom.regex"],
      "matchDepNames": ["nix"],
      "automerge": true
    }
  ],
  "customManagers": [
    {
      "customType": "regex",
      "managerFilePatterns": ["/^\\.github/workflows/.*\\.ya?ml$/"],
      "matchStrings": [
        "install_url:\\s*https://releases\\.nixos\\.org/nix/nix-(?<currentValue>\\d+\\.\\d+\\.\\d+)/install\\s*#\\s*renovate:\\s*datasource=(?<datasource>\\S+)\\s+depName=(?<depName>\\S+)\\s+packageName=(?<packageName>\\S+)"
      ]
    },
    {
      "customType": "regex",
      "managerFilePatterns": ["/^flake\\.nix$/"],
      "matchStrings": [
        "github:cachix/git-hooks\\.nix/(?<currentDigest>[0-9a-f]{40})"
      ],
      "currentValueTemplate": "master",
      "datasourceTemplate": "git-refs",
      "depNameTemplate": "cachix/git-hooks.nix",
      "packageNameTemplate": "https://github.com/cachix/git-hooks.nix"
    }
  ]
}
